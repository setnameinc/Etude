package com.setnameinc.etude.mainschedule.ui

/*
class ScheduleSubjectViewHolder(
    val view: View
) : ScheduleViewHolder(view = view) {

    private val state : ScheduleSubjectStates = ScheduleSubjectStates.COLLAPSED

    override fun bind(item: ScheduleItem) {
        if (state == ScheduleSubjectStates.COLLAPSED){

        }
        */
/*val subject = item as ScheduleItem.ScheduleSubjectItem
        if (subject.size == 0) {
            view.sizeTextView.visibility = View.GONE
        }*//*

    }
}

enum class ScheduleSubjectStates{
    COLLAPSED,
    EXPANDED
}*/
